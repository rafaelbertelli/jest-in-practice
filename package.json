{
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "npm test -- --watchAll",
    "coverage": "npm run test && http-server ./coverage/lcov-report -i -o -p 9090"
  },
  "devDependencies": {
    "babel-core": "^6.17.0",
    "babel-jest": "^15.0.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0",
    "chai": "^3.5.0",
    "http-server": "^0.11.2",
    "jest-cli": "^15.1.1"
  }
}
